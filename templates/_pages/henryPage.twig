{% import "/_macros" as macros %}
{% extends "_layouts/main" %}

{% block title %}
    <title>{{ entry.title|default('BSO Performance History') }}</title>
{% endblock %}

{% block content %}
    {% include '_partials/hero' with { hero: entry.hero.one} %}

    <section class="section basicSection -richText -oneColumn">
        <div class="basicSection__content">
            <div class="basicSection__text richText">
                {{ entry.blockContent }}
            </div>
        </div>
    </section>
     

{% endblock %}